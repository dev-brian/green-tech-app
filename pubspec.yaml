name: greentech_app
description: "GREEN TECH - Monitoreo inteligente de cultivos"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # Gráficas para dashboard e histórico
  fl_chart: ^0.68.0

  # Firebase (descomenta cuando conectes tu proyecto real)
  # firebase_core: ^2.31.0
  # firebase_auth: ^4.19.6
  # cloud_firestore: ^4.17.4

  # HTTP para consumir tu API / MQTT bridge de los ESP32
  http: ^1.2.1

  # Formateo de fechas ("Hace 5 seg", etc.)
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/logo.png
